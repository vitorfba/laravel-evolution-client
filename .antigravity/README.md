# .antigravity

Configuration for [Google Antigravity](https://antigravity.google/) on this repository.

- **Primary context:** the agent reads [`AGENTS.md`](../AGENTS.md) at the repo root — the
  single source of truth for architecture, conventions, testing, tooling and the release
  workflow. Keep that file authoritative; do not fork its content here.
- **[`rules/laravel-package.md`](rules/laravel-package.md):** the short, always-on rule set
  (a distilled do/never list + the quality gates).

When conventions change, edit `AGENTS.md`; update the rules file only if the always-on
summary needs to change.
